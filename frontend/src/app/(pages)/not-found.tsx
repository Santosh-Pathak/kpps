'use client'

import React from 'react'
import NotFound from '@/app/not-found'

/**
 * Custom 404 page for the pages layout
 * This ensures the not-found page uses the same layout structure
 */
export default function PagesNotFound() {
   return <NotFound />
}